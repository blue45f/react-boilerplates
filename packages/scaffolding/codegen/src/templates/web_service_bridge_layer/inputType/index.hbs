import React from 'react'
import { Card, Button, Space, Typography, Form, Input, Radio, Affix } from 'antd'

const { Title } = Typography

interface Props {
  content?: string
}

const {{convertName name feature}}: React.FC<Props> = ({ content = '내용을 입력하세요' }) => {
  const [form] = Form.useForm()

  const handleBackButtonClick = () => {
    /* 취소 버튼 클릭 시 처리 */
  }

  const handleSaveButtonClick = () => {
    /* 저장 버튼 클릭 시 처리 */
    form.validateFields().then((values) => {
      console.log('Form values:', values)
    })
  }

  const ActionButtons = () => (
    <Space>
      <Button onClick={handleBackButtonClick}>취소</Button>
      <Button type="primary" onClick={handleSaveButtonClick}>저장</Button>
    </Space>
  )

  return (
    <div style=\{{ padding: '24px' }}>
      <Form form={form} layout="vertical">
        {/* Page Header */}
        <div style=\{{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: 24 }}>
          <div>
            <Title level={3} style=\{{ marginBottom: 8 }}>입력형 페이지</Title>
            <Typography.Text type="secondary">입력형 페이지입니다.</Typography.Text>
          </div>
          <ActionButtons />
        </div>

        <Space direction="vertical" size="middle" style=\{{ width: '100%' }}>
          <Card title="카드 타이틀">
            <Form.Item
              label="라벨"
              name="sampleName1"
              rules={[{ required: true, message: '필수 입력 항목입니다' }]}
            >
              <Input placeholder="sample placeholder" style=\{{ maxWidth: 600 }} />
            </Form.Item>
          </Card>

          <Card title="카드 타이틀">
            <Form.Item
              label="라벨"
              name="sampleName2"
              rules={[{ required: true, message: '필수 입력 항목입니다' }]}
            >
              <Radio.Group>
                <Radio value="SAMPLE_VALUE1">sampleLabel1</Radio>
                <Radio value="SAMPLE_VALUE2">sampleLabel2</Radio>
              </Radio.Group>
            </Form.Item>

            <Form.Item
              label="라벨"
              name="sampleName3"
              rules={[{ required: true, message: '필수 입력 항목입니다' }]}
            >
              <Input placeholder="sample placeholder" style=\{{ maxWidth: 600 }} />
            </Form.Item>
          </Card>
        </Space>

        {/* NOTE: BottomActionBar는 Card가 5개 이상이라 콘텐츠가 길어지는 경우에 사용해주세요. */}
        <Affix offsetBottom={0}>
          <div style=\{{
            background: '#fff',
            padding: '16px 24px',
            borderTop: '1px solid #f0f0f0',
            textAlign: 'right',
            marginTop: 24
          }}>
            <ActionButtons />
          </div>
        </Affix>
      </Form>
    </div>
  )
}

export default {{convertName name feature}}
