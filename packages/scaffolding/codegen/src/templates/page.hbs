import React from 'react'

import {{convertName name feature}} from "../services/{{feature}}/pages/{{convertName name feature}}"

function {{convertName name feature}}Page(): React.FC {
    return <{{convertName name feature}} />
}

export default {{convertName name feature}}Page
